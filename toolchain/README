This is a cross-development system for PDP-11, ported from 2.11BSD.
It includes:

  * as		- assembler
  * ccom	- Ritchie's C compiler
  * mip, pcc	- Johnson's portable C compiler
  * cpp		- C preprocessor
  * c2		- optimizer for assembler output of C compilers
  * cc		- compiler front end: "cc" and "pcc"
  * ld		- linker, archiver, disassembler, and other a.out utilities
  * crt		- runtime library for long and unsigned arithmetics

To build and install to /usr/local/bin, run:

    make
    make install
___
Serge Vakulenko, <serge@vak.ru>
